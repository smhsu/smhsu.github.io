---
import type Publication from "../Publication";

interface Props {
	publication: Publication;
}

const publicationsBasePath: string = import.meta.env.BASE_URL + (import.meta.env.PUBLICATIONS_DIR || "");
const pub = Astro.props.publication;
---
<p class="font-bold">{pub.title}</p>
<p>{pub.authors}</p>
<p class="italic">In {pub.journal}</p>
{pub.note && <p>{pub.note}</p>}
<div class="flex gap-1">
	{pub.url && <a href={pub.url} target="_blank">[Publisher site]</a>}
	{pub.pdfName && <a href={publicationsBasePath + pub.pdfName} target="_blank">[PDF]</a>}
</div>

<style>
	a {
		@apply text-blue-600;
	}
</style>
